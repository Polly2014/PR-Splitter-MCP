# ============================================
# PR-Splitter-MCP Configuration
# ============================================

# Git configuration
DEFAULT_REMOTE=origin
DEFAULT_BASE_BRANCH=main

# Split configuration
DEFAULT_PR_COUNT=8
SPLIT_STRATEGY=by_module

# ============================================
# AUTHENTICATION - Zero Config!
# ============================================
# 
# This tool uses native SDKs with system credentials.
# Same pattern as professional tools like coding-flow.
#
# Azure DevOps:
#   Uses ChainedTokenCredential(AzureCliCredential, InteractiveBrowserCredential)
#   Just run: az login
#
# GitHub:
#   Token from: GITHUB_PAT_TOKEN > GITHUB_TOKEN > `gh auth token`
#   Just run: gh auth login
#
# No manual token configuration needed!
# ============================================
